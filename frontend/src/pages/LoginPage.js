import React from 'react';
import { useNavigate } from 'react-router-dom';

function LoginPage() {
    return (
        <div>
            <h1>Login page</h1>
        </div>
    );
}

export default LoginPage;
